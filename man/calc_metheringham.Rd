% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classical_reach_models.R
\name{calc_metheringham}
\alias{calc_metheringham}
\title{Reach and exposure distribution under the Metheringham model}
\usage{
calc_metheringham(audiences, insertions, duplication_matrix, population)
}
\arguments{
\item{audiences}{Numeric vector with the audience of each vehicle, in people
per insertion.}

\item{insertions}{Non-negative integer vector with the number of insertions
planned in each vehicle. At least two insertions are required in total.}

\item{duplication_matrix}{Symmetric numeric matrix, in people. Element
\verb{[i, j]} with \code{i != j} is the audience duplicated between one insertion in
vehicle \code{i} and one insertion in vehicle \code{j}. The diagonal element
\verb{[i, i]} is the audience duplicated between two insertions in the same
vehicle \code{i}, that is, \code{2 * audiences[i]} minus the audience accumulated
by two insertions in vehicle \code{i}. Diagonal elements are needed only for
vehicles with at least two insertions, and elements involving a vehicle
with no insertions are ignored.}

\item{population}{Population size, in people.}
}
\value{
A list of class \code{"reach_metheringham"} with components:
\itemize{
\item \code{reach}, \code{distribution}, \code{cumulative} and \code{parameters}, as in
\code{\link[=calc_beta_binomial]{calc_beta_binomial()}} evaluated for \code{N} insertions;
\item \code{mean_audience}: insertion-weighted mean audience (\code{A1}), in
people;
\item \code{mean_duplication}: pair-weighted mean duplication (\code{D}), in
people;
\item \code{second_audience}: mean audience accumulated by two insertions of
the average vehicle (\code{A2}), in people;
\item \code{opportunity_matrix}: number of pairs of insertions between and
within vehicles;
\item \code{opportunity_vector} and \code{duplication_vector}: the upper triangles
of the opportunity and duplication matrices, linearized row by row;
\item \code{total_insertions}: the number of insertions \code{N}.
}
}
\description{
Implements Metheringham's (1964) model for a plan with several vehicles
and several insertions per vehicle, in the version of Aldás Manzano (1998).
Individuals are heterogeneous, with Beta-distributed exposure
probabilities; vehicles are treated as homogeneous, so that duplication
between different vehicles is equivalent to accumulation within one
hypothetical "average" vehicle. The audience and the duplication of that
average vehicle are the averages, over all pairs of insertions, of the
observed audiences and duplications. They fix the two parameters of a
Beta-Binomial distribution for the plan's \code{N = sum(insertions)}
insertions.
}
\details{
There are \code{N = sum(insertions)} insertions and \code{choose(N, 2)} pairs of
insertions: \code{insertions[i] * insertions[j]} pairs between vehicles \code{i} and
\code{j}, and \code{choose(insertions[i], 2)} pairs within vehicle \code{i}. The model
computes
\enumerate{
\item the insertion-weighted mean audience
\eqn{\bar{A}_1 = \sum_i n_i A_i / N};
\item the pair-weighted mean duplication \eqn{\bar{D}}, over all pairs of
insertions;
\item the mean audience accumulated by two insertions of the average
vehicle, \eqn{\bar{A}_2 = 2 \bar{A}_1 - \bar{D}}, which equals the
average, over all pairs of insertions, of the audience reached by the
pair;
\item alpha and beta by the Beta-Binomial estimators of
\code{\link[=calc_beta_binomial]{calc_beta_binomial()}}, and the exposure distribution for \code{N}
insertions.
}
The exposure distribution therefore ranges from zero to \code{N} exposures, and
its mean equals the plan's gross number of exposures per person,
\eqn{\sum_i n_i A_i / P}.
}
\examples{
data(metheringham_example)
result <- do.call(calc_metheringham, metheringham_example)
result$reach$percent
result$parameters$alpha

# One insertion per vehicle reduces to Section 3.2.2.9 of Aldas Manzano
# (1998); the diagonal is not needed then
calc_metheringham(
  audiences = c(300000, 400000, 200000), insertions = c(1, 1, 1),
  duplication_matrix = matrix(c(NA, 150000, 90000,
                                150000, NA, 110000,
                                90000, 110000, NA), nrow = 3),
  population = 1000000
)$reach$percent

}
\references{
Aldás Manzano, J. (1998). Modelos de determinación de la cobertura y la
distribución de contactos en la planificación de medios publicitarios
impresos (Models for determining reach and exposure distribution in print
media planning). Doctoral dissertation, Universidad de Valencia, Spain.
Section 3.3.1.5 (several insertions per vehicle, pages 196-198) and
Section 3.2.2.9 (one insertion per vehicle).

Metheringham, R. A. (1964). Measuring the net cumulative coverage of a
print campaign. Journal of Advertising Research, 4(4), 23-28.
\doi{10.1080/00218499.1964.12519751}
}
\seealso{
\code{\link[=calc_beta_binomial]{calc_beta_binomial()}}, called internally, for the univariate model;
\code{\link[=calc_sainsbury]{calc_sainsbury()}} and \code{\link[=calc_binomial]{calc_binomial()}} for models that assume random
duplication.
}
